Sources for the plugin `plugin.applovin.paid`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovin.paid" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
